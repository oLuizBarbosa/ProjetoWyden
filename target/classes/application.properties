# ======================================
# Configura��es do Banco de Dados (MySQL)
# ======================================
spring.datasource.url=jdbc:mysql://localhost:3306/projeto_wyden?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
# Remova em produ��o e use vari�veis de ambiente
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ======================================
# Configura��es JPA/Hibernate
# ======================================
# Em produ��o, use 'validate' ou 'none'
spring.jpa.hibernate.ddl-auto=update
# Desative em produ��o ('false')
spring.jpa.show-sql=true
# Apenas para dev
spring.jpa.properties.hibernate.format_sql=true
# Em produ��o, use 'validate' ou 'none'
spring.jpa.properties.hibernate.jdbc.batch_size=30

# ======================================
# Configura��es do FreeMarker (Templates)
# ======================================
spring.freemarker.template-loader-path=classpath:/templates/
spring.freemarker.suffix=.ftl
spring.freemarker.cache=false
spring.freemarker.charset=UTF-8
spring.freemarker.check-template-location=true
spring.freemarker.expose-request-attributes=true
spring.freemarker.expose-spring-macro-helpers=true
spring.freemarker.expose-session-attributes=true
spring.freemarker.request-context-attribute=request
spring.freemarker.settings.datetime_format=dd/MM/yyyy HH:mm
spring.freemarker.settings.date_format=dd/MM/yyyy
spring.freemarker.settings.time_format=HH:mm
spring.freemarker.settings.classic_compatible=true

# ======================================
# Configura��es de Seguran�a (Spring Security)
# ======================================
# Ative CSRF (em produ��o, configure cookies seguros)
spring.security.csrf.enabled=true
spring.security.csrf.token-repository=cookie

# Configura��es de sess�o (HTTPS obrigat�rio em produ��o)
server.servlet.session.timeout=30m
server.servlet.session.cookie.http-only=true
# Ative ('true') se usar HTTPS
server.servlet.session.cookie.secure=false

# ======================================
# Logging (Ajuste conforme ambiente)
# ======================================
# Para dev
logging.level.com.wyden.ProjetoWyden=DEBUG
# Reduza logs de seguran�a
logging.level.org.springframework.security=WARN
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# ======================================
# Configura��es Espec�ficas para Produ��o (Exemplo)
# ======================================
# spring.profiles.active=prod
# spring.jpa.show-sql=false
# spring.freemarker.cache=true
# server.servlet.session.cookie.secure=true